import * as React from 'react'

import { CenterElement } from './index.style'

const PlaceHolderComponent: React.FC = ({ children }) => {
  return <CenterElement>{children}</CenterElement>
}
export default PlaceHolderComponent
